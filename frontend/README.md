# Fast Talk - Чат приложение на Vue.js

Полноценное чат-приложение с регистрацией, личными и групповыми чатами, голосовыми сообщениями и реальным временем через WebSocket.

## 🚀 Возможности

- ✅ Регистрация и авторизация пользователей
- ✅ Личные чаты (1 на 1)
- ✅ Групповые чаты
- ✅ Голосовые сообщения (запись и воспроизведение)
- ✅ Реальное время через WebSocket
- ✅ Современный UI с Tailwind CSS
- ✅ Адаптивный дизайн

## 📋 Требования

- Node.js 18+ 
- npm или yarn
- Backend API (FastAPI) должен быть запущен на `http://localhost:8000`

## 🛠️ Установка

1. Установите зависимости:
```bash
npm install
```

2. Запустите dev-сервер:
```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

## 📦 Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

## 🏗️ Структура проекта

```
frontend/
├── src/
│   ├── components/          # Vue компоненты
│   │   ├── ChatHeader.vue
│   │   ├── MessageInput.vue
│   │   ├── MessageList.vue
│   │   ├── MessageItem.vue
│   │   ├── CreateGroupModal.vue
│   │   └── UserListModal.vue
│   ├── views/               # Страницы
│   │   ├── Login.vue
│   │   ├── Register.vue
│   │   ├── ChatList.vue
│   │   ├── PrivateChat.vue
│   │   └── GroupChat.vue
│   ├── layouts/             # Layout компоненты
│   │   └── MainLayout.vue
│   ├── stores/              # Pinia stores
│   │   ├── auth.js
│   │   └── chat.js
│   ├── services/            # API и WebSocket сервисы
│   │   ├── api.js
│   │   └── websocket.js
│   ├── router/              # Vue Router
│   │   └── index.js
│   ├── App.vue
│   ├── main.js
│   └── style.css
├── index.html
├── package.json
├── vite.config.js
└── tailwind.config.js
```

## 🔌 API Endpoints (ожидаемые)

Приложение ожидает следующие endpoints на backend:

### Авторизация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Получить текущего пользователя

### Чаты
- `GET /api/chats` - Список чатов
- `GET /api/chats/:id` - Получить чат
- `POST /api/chats/private` - Создать приватный чат
- `POST /api/chats/group` - Создать групповой чат
- `GET /api/chats/:id/messages` - Получить сообщения

### Пользователи
- `GET /api/users` - Список пользователей

### WebSocket Events

**Клиент → Сервер:**
- `join_chat` - Присоединиться к чату
- `leave_chat` - Покинуть чат
- `send_message` - Отправить сообщение

**Сервер → Клиент:**
- `message` - Новое сообщение
- `chat_created` - Создан новый чат
- `user_online` - Пользователь онлайн
- `user_offline` - Пользователь офлайн

## 🎨 Технологии

- **Vue 3** - Progressive JavaScript framework
- **Vite** - Next generation frontend tooling
- **Vue Router** - Official router for Vue.js
- **Pinia** - State management
- **Tailwind CSS** - Utility-first CSS framework
- **Socket.io-client** - WebSocket клиент
- **Axios** - HTTP клиент

## 📝 Лицензия

MIT

